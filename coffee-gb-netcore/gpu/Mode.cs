namespace eu.rekawek.coffeegb.gpu
{
    public class Gpu // This needs removing but temp.
    {
        public enum Mode
        {
            HBlank,
            VBlank,
            OamSearch,
            PixelTransfer
        }
    }
}
